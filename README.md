webapp foler contains all the html ,xml ,controllers, css etc
TODO : 

Step-1:Initializing

installing `ui5/cli`

```bash
npm i -D @ui5/cli
```

initilalize cli

```bash
ui5 init
```

Step-2:Bootstrapping : Adding UI5 to HTML
1. What is SAP UI5 bootstrapping
2. How UI5 loads into the browser
3. What each bootstrap sttribute does
4. How UI5 resolves namespaces
5. How to run UI5 modules
6. How to trigger an action when UI5 is ready
7. Moving JS from HTML
8. How CLI helps serve the framework

Update the UI5 CLI settings 

```bash
ui5 use OpenUi5
```

Adding ui5 libraries 
```bash
ui5 add sap.ui.core sap.m themelib_sap_horizon
```